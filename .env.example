# Origin-provided API key for the take-home (Anthropic). Required for the
# best output; the agent falls back to a deterministic regex extractor if
# this is unset, but draft quality and Spanish handling will be weaker.
ANTHROPIC_API_KEY=
